import 'package:flutter/material.dart';

class ColorConst{

  static const primaryColor = Colors.blueAccent;
  static const secondaryColor = Colors.lightBlueAccent;
  static const greyColor = Colors.grey;
  static const darkGreyColor = Color(0xFF626161);
  static const whiteColor = Color(0xFFFFFFFF);
  static const blackColor = Color(0xFF000000);

  // OnBoarding Screen
  static const darkActiveColor = Colors.white;
  static const lightActiveColor = Colors.black;
  static const dotColor = Colors.grey;
}